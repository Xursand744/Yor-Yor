import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "uz.yoryor.app",
  appName: "Yor Yor",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
