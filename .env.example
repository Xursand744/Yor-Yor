# SQLite (ishlab chiqish uchun — qo'shimcha dastur kerak emas)
DATABASE_URL="file:./prisma/dev.db"

# NextAuth (server — npm run dev / npm run build)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="o'zgartiring-kuchli-random-secret-32-belgidan-kam-bo'lmasin"

# APK / static frontend (npm run static — build vaqtida .env yoki .env.local)
# Production API — barcha fetch va NextAuth (next.config env orqali):
# NEXT_PUBLIC_API_URL="https://api.yoryor.uz"
# NEXT_PUBLIC_NEXTAUTH_URL="https://api.yoryor.uz"
# NEXTAUTH_URL="https://api.yoryor.uz"

# Production uchun PostgreSQL (ixtiyoriy):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/toy24?schema=public"
